"""Pure computation: the sandbox launcher, the child runner, and the bounds they share.

No FastAPI, no MCP, no network — the same one-way import rule every server in this fleet follows,
and the reason these modules are testable without a transport installed.
"""
