# The `suitability` bundle, as Chemclaw3 reads it.
#
# A connector Chemclaw3 does **not** run (D-2026-08-09-a-connector-we-do-not-run): there is no
# `server/` half in the Chemclaw3 tree, only this manifest and an address. Drop this directory onto
# `CHEMCLAW_CONNECTORS_DIR` and the capability appears with no core edit.
#
# The URL below is the loopback dev default. A cluster address is set per deployment, by
# `CHEMCLAW_CONNECTOR_URLS` or by `connectors.suitability.url` in the Helm chart.
name: suitability
description: >-
  USP <621> chromatographic system-suitability arithmetic, from numbers a chromatogram already
  reported: replicate-injection RSD with the compendial rule for how many injections that limit
  needs, theoretical plate count and resolution under both width conventions USP defines, the
  tailing factor at 5% height, retention and separation factors, and whether a proposed change to
  an isocratic method stays inside what the general chapter permits without revalidation. Nothing
  here integrates a chromatogram, finds a peak, assigns a baseline or opens an instrument file —
  every input is a retention time, a width or an area a person or a data system already reported.
  Passing suitability says the system performed at the moment of the run; it is not method
  validation and not evidence the result is accurate. Pure and synchronous — no store, no durable
  state, no corpus, and no network call at any point.

endpoint:
  transport: http
  # A loopback dev default; the deployment moves it.
  url: http://127.0.0.1:8892/mcp
  health_url: http://127.0.0.1:8892/healthz
  # Every tool here is microseconds of closed-form arithmetic. The bound exists to stop a mute
  # connector hanging a turn, not to police a fast one.
  request_timeout: 15
  # Bearer even though the shipped URL is loopback, for the reason `servers/props/connector.yaml`
  # states: a manifest whose auth mode changes with its address is one whose serving side gets it
  # wrong. The token env var is the same name on both sides.
  auth:
    mode: bearer
    token_env: CHEMCLAW_SUITABILITY_TOKEN
  tools:
    - replicate_precision
    - plate_count
    - peak_symmetry
    - peak_resolution
    - retention_factor
    - permitted_method_adjustment
    - system_suitability_report

  # Every tool is read-only: each takes numbers, computes, and returns. Nothing here writes, stores
  # or reserves anything, so there is no state a plan gate would need to approve. Classifying by
  # omission fails *open* (D-167), which is why all seven are listed rather than left to a default.
  read_only:
    - replicate_precision
    - plate_count
    - peak_symmetry
    - peak_resolution
    - retention_factor
    - permitted_method_adjustment
    - system_suitability_report
